var expect = require('chai').expect;
var shapefile_to_oada_rx = require("shapefile_to_oada_rx");

var testconfig = {
  "example_shapefile": "example_shapefile.shp",
  "example_oadarx": "example_oadarx.json"
};

describe("shapefile_to_oada_rx", function() {
  it('should read the test shapefile and return a valid shapefile object', function() {

    expect(false).to.be.true;
  };
  it('should convert the shapefile object into a valid oada RX object', function() {
    expect(false).to.be.true;
  };
  it('should write an output file which matches the example output file byte-for-byte', function() {
    expect(false).to.be.true;
  };
});

